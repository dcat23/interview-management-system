# Alternate/local Verdaccio registry (e.g. an ngrok tunnel to a local instance).
# Comment out the line below to fall back to the registry configured in ~/.npmrc.
# registry=https://broaden-decidable-pavestone.ngrok-free.dev
